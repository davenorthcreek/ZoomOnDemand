
(function () {
    'use strict';

    angular
        .module('zoomApp')
        .controller('ProfileController', ProfileController);

    /** @ngInject */
    ProfileController.$inject = ['$state', '$scope'];
    function ProfileController($state, $scope) {
        // var vm = this;        
     
    }
})();